class Message {
    constructor(text = '') {
        this._text = text;
    }

    get text() {
        return this._message;
    }

    set text(text) {
        this._message = text;
    }
}
//# sourceMappingURL=Message.js.map